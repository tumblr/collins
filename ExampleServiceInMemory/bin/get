#!/bin/sh

if [ $# = 1 ]; then
    uri='http://0.0.0.0'
    key=$1
elif [ $# = 2 ]; then
    uri=$1
    key=$2
fi

if [[ $# > 0 && $# < 3 ]]; then
   curl -s $uri:8888/$key
else
   echo "Wrong number of arguments"
fi